doctype html

block vars
include ../components/_mixins

html(lang="ru")
	head
		meta(charset="UTF-8")
		meta(name="viewport", content="width=device-width, initial-scale=1.0")
		meta(name="description" content="")
		meta(name="keywords" content="")

		include ../partials/__preload-styles

		//-link(href="css/main.min.css" rel="stylesheet" media="print" onload="this.media='all'")
		noscript
			link(href="css/main.min.css" rel="stylesheet")

		link(rel="preload" href="images/screen1/pattern.webp" as="image")

		title= `${page.title}`

	body.disable-scroll.js-light
		.wrapper
			block header
				include ../partials/_header

			main.main
				block main

			block footer
				include ../partials/_footer

		block modals


		script
			| window.addEventListener('load', function() {
			|		var link = document.createElement('link');
			|			link.rel = 'stylesheet';
			|		link.href = '/css/main.min.css';
			|		document.head.appendChild(link);
			| });

		script(src="js/main.min.js")

		block scripts
